[package]
name = "kvs"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.7", features = ["derive"] }
tempfile = "3.27.0"
walkdir = "2.5.0"

[dev-dependencies]
assert_cmd = "0.11.0"
predicates = "1.0.0"
